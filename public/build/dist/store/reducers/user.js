export default (function (state, action) {
    if (state === void 0) { state = []; }
    switch (action) {
        case '': {
            return state;
        }
        default: {
            return state;
        }
    }
});
//# sourceMappingURL=user.js.map